const cv = {}

export function log(...args) {
	console.log(...args);
}

export const c = {
	a: async function* yoyo() {
		
	},
	cv
}

export interface c<K, V> extends Map<K, V> {
	apple: string;
	v: () => Promise<true>;
}

let count = 0;
count += 5

if ((count += 5) === 10) {
	console.log('this is poggers');
}

console.log('Loaded a ts file;', __filename);