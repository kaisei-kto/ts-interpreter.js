'use strict';

import './src/classes';
import './src/type-arguments';

export function whoami(a?: string): string {
	function t(): Promise<abcd> {}
	return 'ts-interpreter.js';
}

console.log('Loaded a ts file;', __filename);
